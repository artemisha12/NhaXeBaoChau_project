// PriceTable.tsx — Bảng giá nhóm theo tuyến đường
// Fetch GET /api/packages → group by route
// Hiển thị 2 cột: xe ghép (shared) | bao chuyến (charter)
export default async function PriceTable() { return null }
